interface:
  display_name: "JHSTE Subagent Orchestration"
  short_description: "Choose the lightest useful worker topology"
  default_prompt: "Use $jhste-subagent-orchestration to choose the lightest useful worker topology, preserve head-owned decisions and ownership, and verify every result without changing harness runtime settings."

policy:
  allow_implicit_invocation: true
