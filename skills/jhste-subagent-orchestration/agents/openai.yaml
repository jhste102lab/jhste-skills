interface:
  display_name: "JHSTE Subagent Orchestration"
  short_description: "Coordinate bounded workers under head-owned decisions"
  default_prompt: "Use $jhste-subagent-orchestration to coordinate bounded worker assignments under head-owned decisions, isolated mutation leases, and head verification."

policy:
  allow_implicit_invocation: true
