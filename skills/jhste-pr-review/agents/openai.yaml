interface:
  display_name: "JHSTE PR Review"
  short_description: "Discover PR defects, then post verified findings"
  default_prompt: "Use $jhste-pr-review to inspect this pull request broadly, validate each candidate against the actual diff and execution path, and post only verified material findings."

policy:
  allow_implicit_invocation: true
